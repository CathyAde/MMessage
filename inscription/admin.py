from .models import member
from django.contrib import admin

# Register your models here.
# @admin.register(member)
# class memberAdmin(admin.ModelAdmin):
#     liste_display = ('id', 'name', 'email', 'password')